# {{ ansible_managed }}

stream {
    upstream bedrock {
        server localhost:{{ bedrock.port }};
        server {{ balancer.floating_ip }}:{{ bedrock.port }} backup;
    }

    server {
        listen 9999;
        proxy_pass bedrock;
    }
}
